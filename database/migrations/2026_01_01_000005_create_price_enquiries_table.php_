<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * A "Check Price" lead — matches the website's Quick Price Check /
 * hero form and the Flutter Check Price flow. This is Dev Parcel's
 * lead-generation mechanism, so every submission is stored even if the
 * customer never books.
 */
return new class extends Migration {
    public function up(): void
    {
        Schema::create('price_enquiries', function (Blueprint $table) {
            $table->id();
            $table->foreignId('customer_id')->nullable()->constrained()->nullOnDelete();
            $table->string('mobile', 10);
            $table->string('pickup_city');
            $table->string('delivery_city');
            $table->string('goods_type')->nullable();
            $table->string('weight')->nullable();
            $table->unsignedInteger('packages')->nullable();
            $table->unsignedInteger('estimated_min')->nullable();
            $table->unsignedInteger('estimated_max')->nullable();
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('price_enquiries');
    }
};
